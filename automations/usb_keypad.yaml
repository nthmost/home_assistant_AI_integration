# USB Wireless Numpad Configuration for Home Assistant
# Device: Compx 2.4G Wireless Receiver
#
# Uses the built-in keyboard_remote integration
# Fires keyboard_remote_command_received events for automations

device_descriptor: /dev/input/by-id/usb-Compx_2.4G_Wireless_Receiver-event-kbd
type: key_down

# Note: Keypad automations are now in keypad_pin_code.yaml
# PIN: 123 + Enter = Turn ON Tube Lamp
